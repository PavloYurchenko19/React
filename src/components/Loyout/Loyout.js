import React from 'react';
import {NavLink} from "react-router-dom";

const Loyout = () => {
    return (
        <div>
            <NavLink to={'/' } >Home</NavLink>

        </div>
    );
};

export {Loyout};