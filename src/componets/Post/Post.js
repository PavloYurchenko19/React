import React from 'react';

const Post = ({post}) => {
    const {id,title} = post
    return (
        <div>
            {
                {id}
                h

            }
        </div>
    );
};

export default Post;