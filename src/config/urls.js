const baseURL = 'https://jsonplaceholder.typicode.com/';

export default baseURL;

export const urls = {
    users: '/users',
    posts: '/posts',
    comments: '/comments',
    alboms: '/albums',
    foto: '/photos'
}