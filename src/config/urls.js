const baseURL = 'https://jsonplaceholder.typicode.com/';

export default baseURL;

export const urls = {
    user: '/users',
    posts: '/posts',
    comments: '/comments',
    alboms: '/albums',
    foto: '/photos'
}